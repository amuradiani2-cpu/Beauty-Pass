const express = require('express');
const router = express.Router();

// Community routes placeholder - expand as needed
router.get('/status', (req, res) => {
  res.json({ message: 'Community API is running' });
});

module.exports = router;
