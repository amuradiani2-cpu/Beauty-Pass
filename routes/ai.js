const express = require('express');
const router = express.Router();

// AI routes placeholder - expand as needed
router.get('/status', (req, res) => {
  res.json({ message: 'AI API is running' });
});

module.exports = router;
